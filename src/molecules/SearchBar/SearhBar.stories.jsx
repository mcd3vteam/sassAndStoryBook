import { SearchBar } from ".";

export default {
  title: "Molecules/Search Bar",
};

const SearchBarComponent = () => {
  return <SearchBar />;
};
export const Default = SearchBarComponent.bind({});
